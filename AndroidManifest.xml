<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.xconns.samples.chat"
    xmlns:tools="http://schemas.android.com/tools"
    android:versionCode="4"
    android:versionName="1.4" >

    <uses-sdk
        android:minSdkVersion="8"
        android:targetSdkVersion="19" />

    <uses-permission android:name="com.xconns.peerdevicenet.permission.REMOTE_MESSAGING" />

    <application
        android:allowBackup="true"
        android:icon="@drawable/ic_launcher"
        android:label="@string/app_name"
        android:theme="@style/AppTheme"
        tools:replace="android:icon" >
        <activity
            android:name=".ChatActivity"
            android:label="@string/app_name" >
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".ChatByIntentingActivity" >
        </activity>
        <activity android:name=".ChatByMessengerActivity" >
        </activity>
        <activity android:name=".ChatByAidlActivity" >
        </activity>
    </application>

</manifest>
